git add . #adds all changes to the staging area

git commit -m "Your commit message" #commits the changes with a message

git push origin <remote-branch-name> #pushes the changes to the specified remote branch

git switch <branch-name> #switches to the specified branch

# pip command 
#freeze the current environment's packages and their versions into a requirements.txt file
pip freeze > requirements.txt